import React from 'react'
import './globals.css'

// Root layout — minimal wrapper
// Each route group ((payload) and (frontend)) has its own layout
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children
}
